<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>FARMEASY</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #43a047, #8bc34a);
      color: white;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      overflow: hidden;
    }
    h1 {
      font-size: 4rem;
      letter-spacing: 3px;
      animation: fadeIn 1.5s ease forwards;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    button {
      margin-top: 40px;
      padding: 18px 40px;
      font-size: 1.3rem;
      background: white;
      color: #2e7d32;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: transform 0.3s ease, background 0.3s ease;
    }
    button:hover {
      transform: scale(1.08);
      background: #2e7d32;
      color: white;
    }
  </style>
</head>
<body>
  <h1>🌾 FARMEASY</h1>
  <p>Empowering farmers to sell directly to consumers and businesses.</p>
  <button onclick="window.location.href='/register'">Register Farmer</button>
</body>
</html>